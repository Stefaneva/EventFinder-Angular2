export class User {
  email: string;
  name: string;
  type: string;
  accessToken: string;
  refreshToken: string;
  phone: number;
  enabled: boolean;
  lastLoginDate: string;
  notification: number;
}
